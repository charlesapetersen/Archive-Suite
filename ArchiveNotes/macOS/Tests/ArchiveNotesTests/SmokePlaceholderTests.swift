import XCTest
@testable import ArchiveNotes

final class SmokePlaceholderTests: XCTestCase {
    func testAppExists() {
        // Placeholder so `xcodebuild test` has a runnable suite.
        // Real suites arrive in W2+ / W8.
        XCTAssertTrue(true, "Archive Notes target builds and links")
    }
}
