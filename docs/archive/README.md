# docs/archive

Shipped / historical planning docs, kept for reference (not deleted — full history is also in git).
Moved here in the 2026-07-06 documentation-consolidation pass so the app folders show only their
**live** docs (CLAUDE · README · AGENTS · KNOWN_ISSUES · POTENTIAL_FEATURES).

- `SUITE_MERGE_PLAN.md` — the Archive Reader + Processor → Archive Suite merge (phases A–F, all done).
- `ArchiveReader/PLAN_NEAR_TERM_UI.md`, `NEAR_TERM_UI.md`, `UI_REFINEMENTS.md` — the two shipped Reader UI batches.
- `ArchiveReader/STATUS.md` — point-in-time Reader status snapshots (superseded by the current code + README).

Reader's `PLAN.md` stayed live (it still holds the keyboard map, options list, and edge-case rules the
app docs cite). The active backlog is the root `SUITE_TODO.md`; the P3 plan is `P3_PLAN.md`.
