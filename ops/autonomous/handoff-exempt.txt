# Tags that are deliberately in NEITHER plan queue region, so check-handoff.sh step 3 must not fail on them.
#
# Step 3's rule — "every open SUITE_TODO item must be visible in the plan" — is right for the 27-item gap it
# was written for, but wrong for an item whose own spec forbids BOTH regions. That is a real category, not a
# loophole: an item can be out of scope until a QUALITATIVE bar is met, which is different from awaiting an
# owner gate. Parking such an item in the HOLD QUEUE mislabels it and makes the "held back" count lie.
#
# ⛔ Adding a tag here is a policy act. Only add one when the item's OWN spec in SUITE_TODO.md explicitly says
# it must not be mirrored, and cite that line. Never add one to silence the check.
#
# format:  <tag><TAB or spaces><SUITE_TODO.md citation> — <reason>
Import   SUITE_TODO.md §"Archive Notes — DEVONthink import" (find it by the SECTION HEADING, not a line number — the citation written here as ~L1376 had drifted 19 lines within three commits) ⚠️ NB the key `Import` is this bullet's first WORD, not a tag, so a second open bullet beginning "Import" would be silently swallowed by this exemption (first-occurrence-wins). If one is ever filed, give it a real tag. — "never mirror it into …WORK QUEUE… Do not put it in the plan's HOLD QUEUE either: it is not awaiting an owner *gate*, it is out of scope until a qualitative bar is met." Owner directive 2026-08-01; plans retained in full.
W13.cli  SUITE_TODO.md §"Provider expansion (Wave 13)", the ⏸ PARKED block inside the W13.cli entry — "⏸ PARKED by the owner 2026-08-13 … Do NOT install them, do not chase the entitlement spike, and do not re-raise this." ⚠️ Honest caveat, since this file's own rule demands the spec say it must not be mirrored: the entry's metadata column reads `owner` (he must install the CLIs and sign in), so a HOLD QUEUE line would ALSO be defensible. It is exempt rather than parked-in-HOLD because he asked for it not to be chased at all, and a HOLD entry would keep it in the "held back for you to decide" count as though a decision were outstanding. Nothing is blocked: the fake-CLI harness covers the Local Agent path at $0 and `claude` is Phase-0-validated. Un-park only if he asks.
